
config SP804
	bool "DesignWare APB UART"
	depends on VEXPRESS
	
config SUN4I_TIMER
	bool "SUN4i timer"
	depends on PINE64 || A20 || MERIDAv2 || BPI

	
config ARM_CLOCKSOURCE
	bool "ARM architected CP15 clocksource"
